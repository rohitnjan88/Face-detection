-------------------------------------------------------------------

 This is c++ program to detect face from the images taken from your web cam
 

 compile the code as

 g++ facedetect -o detect_me `pkg-config opencv --cflags --libs`


run as 
   ./detect_me'

----------------------------------------------------------------
